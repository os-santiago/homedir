# New UI Archive

This folder stores an archived static prototype for reference.

Contents:

- `index.html`: historical static artifact used during UI exploration.

Notes:

- It is not the active production UI.
- Keep this archive lightweight and avoid adding runtime dependencies.

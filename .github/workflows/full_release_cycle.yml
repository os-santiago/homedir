name: Full Release Cycle (DEPRECATED)

# ⚠️ DEPRECATED: This workflow is obsolete.
# Use the canonical "Production Release" workflow (.github/workflows/release.yml) instead.
#
# Migration guide:
# - For automatic releases: push to main (release.yml triggers automatically)
# - For manual releases: use workflow_dispatch on release.yml with release_level input
# - For QA verification without deploy: use release.yml with skip_deploy=true
#
# Issue #865 unified all release paths into release.yml to ensure consistent gates.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'DEPRECATED - Use release.yml instead'
        required: false
        default: ''

jobs:
  deprecation-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Warning
        run: |
          echo "::error::This workflow is deprecated and will be removed."
          echo "::error::Use .github/workflows/release.yml instead for all release operations."
          echo "::error::See docs/ci/RELEASE.md for the canonical release process."
          exit 1
